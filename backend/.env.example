# Application
APP_NAME=Promethean
ENVIRONMENT=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Database (required) — async Postgres URL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/promethean

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000
